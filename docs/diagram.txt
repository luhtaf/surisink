
Suricata (eve.json + filestore)
  |
  |  tail eve.json (event_type=fileinfo, stored=true)
  v
surisink
  ├─ resolve path (file_id -> filestore/file.<id> | absolute path)
  ├─ compute sha256, guess MIME
  ├─ upload to S3 (prefix=suricata/YYYY/MM/DD/flow_id/sha256_orig)
  └─ set tags: sha256, mime, ts, flow_id, src, dst
